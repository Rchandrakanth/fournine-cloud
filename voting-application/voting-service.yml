apiVersion: v1
kind: Service
metadata:
  name: voting-service 
spec:
  type: NodePort
  selector:
    name: voting-pod
    app: voting-application-app
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30005


