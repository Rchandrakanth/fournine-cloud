apiVersion: v1
kind: Service
metadata:
  name: result-service
spec:
  type: NodePort
  selector:
    name: result-pod 
    app: voting-application-app
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30006
