apiVersion: v1
kind: Pod
metadata:
  name: voting-pod
  labels:
    name: voting-pod
    app: voting-application-app
spec:
  containers:
  - name: voting-container
    image: chandu22727/examplevotingapp_vote
    ports:
    - containerPort: 80

